{
  "name": "grok-bot-second-brain",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types --test-reporter=spec src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
